exports.getAddProduct = (req, res, next) => {
    var errormsg = req.flash('error');
    if (errormsg.length > 0) {
        errormsg = errormsg[0];
    } else {
        errormsg = null;
    }
    res.render('UserPages/add-product',{path:'/add-product',error: errormsg});
}